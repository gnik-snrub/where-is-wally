import StyledHeader from "./StyledHeader"


const Header = (props) => {
    return (
        <StyledHeader>
            <h1 >Where's That Pokemon?</h1>
        </StyledHeader>
    )
}

export default Header